define([
    "jquery",
    "backbone",
    "firebase",
    "backfire"
], function (
    $,
    Backbone,
    Firebase
) {
  "use strict";

    var ListItemModel = Backbone.Model.extend({

    });

    return ListItemModel;

});
